const styles = {
    header: {
        marginTop: 30,
        marginBottom: 30
    },
    container: {
        display: 'flex',
        flexDirection: 'column',
        jutifyContent: 'center',
        alignItems: 'center'
    }
    
}

export default styles